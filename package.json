{
  "name": "thematerial",
  "version": "1.0.1",
  "description": "The material website",
  "main": "index.js",
  "scripts": {
    "start": "babel ./src --out-dir ./docs/compiled --presets @babel/preset-env --source-maps",
    "watch": "babel ./src --out-dir ./docs/compiled --watch --presets @babel/preset-env --source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kraniebrud/thematerial.git"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "8.11.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0"
  },
  "dependencies": {
    "finalhandler": "^1.1.1",
    "node-fetch": "^2.2.1",
    "serve-static": "^1.13.2"
  }
}
