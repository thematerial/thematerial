{
  "name": "thematerial",
  "version": "1.0.1",
  "description": "The material website",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=PRODUCTION node ./server",
    "dev:ip-allow": "NODE_ENV=IP_ALLOW node ./server",
    "dev:ip-deny": "NODE_ENV=IP_DENY node ./server",
    "now:deploy-team": "now -T thematerial",
    "now:deploy": "now",
    "build": "babel ./src --out-dir ./public/compiled --presets @babel/preset-env --source-maps",
    "watch": "babel ./src --out-dir ./public/compiled --watch --presets @babel/preset-env --source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kraniebrud/thematerial.git"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "8.11.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0"
  },
  "dependencies": {
    "finalhandler": "^1.1.1",
    "node-fetch": "^2.2.1",
    "serve-static": "^1.13.2"
  }
}
